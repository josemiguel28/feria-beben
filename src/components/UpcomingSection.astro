---
// UpcomingSection.astro — Previsualización de lo que viene inmediatamente después
// Muestra únicamente el día siguiente (Mañana) + botón secundario "Ver todas las actividades →"
import type { FeriaDay } from "../data/feria";
import ActivityRow from "./ActivityRow.astro";

interface Props {
  days: FeriaDay[];   // días DESPUÉS de hoy, en orden
}

const { days } = Astro.props;

const tomorrow = days[0];

// Full day name for tomorrow
const tomorrowFull = (day: FeriaDay) => {
  const d = new Date(day.date + "T12:00:00");
  const wd = d.toLocaleDateString("es-HN", { weekday: "long" });
  return wd.charAt(0).toUpperCase() + wd.slice(1);
};
---

<section id="proximo" class="upcoming-section" aria-label="Próximas actividades" style="scroll-margin-top: 56px;">
  <div class="container">

    <!-- Header -->
    <div class="upcoming-header">
      <p class="section-eyebrow upcoming-eyebrow">¿Qué viene después?</p>
    </div>

    <!-- ── Mañana (Previsualización única) ─────────── -->
    {tomorrow ? (
      <div class="tomorrow-block">
        <h2 class="tomorrow-title">
          <span class="tomorrow-label">Mañana</span>
          <span class="tomorrow-day">— {tomorrowFull(tomorrow)} {tomorrow.dayNumber}</span>
        </h2>
        <div role="list" aria-label={`Actividades del ${tomorrowFull(tomorrow)} ${tomorrow.dayNumber}`}>
          {tomorrow.activities.map((act) => (
            <div role="listitem">
              <ActivityRow activity={act} compact={true} />
            </div>
          ))}
        </div>
      </div>
    ) : (
      <div class="tomorrow-block">
        <h2 class="tomorrow-title">
          <span class="tomorrow-label">Último día de feria</span>
        </h2>
        <p class="tomorrow-empty-msg">Hoy concluyen las festividades de la feria patronal.</p>
      </div>
    )}

    <!-- ── Botón Secundario: Ver todas las actividades ── -->
    <div class="upcoming-cta-wrap">
      <button
        type="button"
        class="btn-see-all-schedule"
        data-open-schedule
        aria-label="Abrir programa completo con todas las actividades"
      >
        <span>Ver todas las actividades</span>
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="currentColor" class="cta-arrow" aria-hidden="true">
          <path fill-rule="evenodd" d="M6.22 4.22a.75.75 0 0 1 1.06 0l3.25 3.25a.75.75 0 0 1 0 1.06l-3.25 3.25a.75.75 0 0 1-1.06-1.06L8.94 8 6.22 5.28a.75.75 0 0 1 0-1.06Z" clip-rule="evenodd" />
        </svg>
      </button>
    </div>

  </div>
</section>

<style>
  .upcoming-section {
    background-color: var(--color-bg);
    border-top: 1px solid var(--color-border);
    padding-block: 2.75rem 3.25rem;
  }

  /* ─── Header ──────────────────────────────────────── */
  .upcoming-header {
    margin-bottom: 1.25rem;
  }

  .upcoming-eyebrow {
    color: var(--color-muted);
    margin: 0;
  }

  /* ─── Mañana ──────────────────────────────────────── */
  .tomorrow-block {
    margin-bottom: 1.5rem;
  }

  .tomorrow-title {
    font-family: var(--font-body);
    font-size: clamp(1.25rem, 5vw, 1.625rem);
    font-weight: 800;
    color: var(--color-ink);
    margin: 0 0 1rem;
    line-height: 1.2;
    letter-spacing: -0.025em;
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 0 0.5rem;
  }

  .tomorrow-label {
    color: var(--color-ink);
    font-weight: 800;
  }

  .tomorrow-day {
    font-size: 0.8em;
    font-weight: 500;
    color: var(--color-muted);
    font-style: normal;
    letter-spacing: 0;
  }

  .tomorrow-empty-msg {
    font-family: var(--font-body);
    font-size: 0.9375rem;
    color: var(--color-muted);
    margin: 0 0 1rem;
  }

  /* ─── Botón Secundario "Ver todas las actividades" ── */
  .upcoming-cta-wrap {
    padding-top: 0.75rem;
  }

  .btn-see-all-schedule {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    width: 100%;
    min-height: 48px;
    padding: 0.75rem 1.25rem;
    background-color: #FFFFFF;
    border: 1px solid var(--color-border);
    border-radius: 8px;
    font-family: var(--font-body);
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-ink);
    cursor: pointer;
    text-decoration: none;
    transition: border-color 0.15s ease, background-color 0.15s ease, color 0.15s ease, transform 0.12s ease;
    letter-spacing: -0.005em;
  }

  .btn-see-all-schedule:hover {
    border-color: #D1D5DB;
    background-color: #F9FAFB;
    color: var(--color-accent);
  }

  .btn-see-all-schedule:active {
    transform: scale(0.985);
  }

  .cta-arrow {
    width: 1rem;
    height: 1rem;
    color: var(--color-accent);
    flex-shrink: 0;
    transition: transform 0.15s ease;
  }

  .btn-see-all-schedule:hover .cta-arrow {
    transform: translateX(3px);
  }

  @media (min-width: 480px) {
    .btn-see-all-schedule {
      width: auto;
      padding-inline: 1.5rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .btn-see-all-schedule,
    .cta-arrow {
      transition: none !important;
      transform: none !important;
    }
  }
</style>
